services:
  web:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/code/data
    environment:
      - REDIS_URL=redis://redis:6379/0
      - GROQ_API_KEY=${GROQ_API_KEY}
    depends_on:
      - redis

  redis:
    image: redis:7
    ports:
      - "6379:6379"
